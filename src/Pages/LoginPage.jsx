import React from 'react';
import LoginForm from '../Component/Login/LoginForm';

const LoginPage = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default LoginPage;